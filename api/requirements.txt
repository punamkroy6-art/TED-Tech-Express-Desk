# TED API — Slim requirements for Vercel serverless (< 200 MB)
# Excluded (hardware-only, not available in serverless):
#   opencv-python-headless  ~100 MB  (camera capture)
#   paramiko                ~20 MB   (SSH remote fix)
#   python-docx + lxml      ~30 MB   (Word reports)
#   pyzbar + Pillow         ~40 MB   (barcode / image)
#   psutil                  ~5 MB    (hardware metrics — limited in serverless)
#   prometheus-fastapi-instrumentator ~10 MB

fastapi==0.111.0
uvicorn==0.30.0
pydantic-settings==2.3.0
sqlalchemy[asyncio]==2.0.31
aiosqlite==0.20.0
python-jose[cryptography]==3.3.0
httpx==0.27.0
groq==0.9.0
slowapi==0.1.9
python-multipart==0.0.9
fakeredis[aioredis]>=2.20.0
redis[asyncio]==5.0.7
pyyaml>=6.0
