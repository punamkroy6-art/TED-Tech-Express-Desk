[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["cd ted/backend && python seed_data.py"]

[start]
cmd = "cd ted/backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
